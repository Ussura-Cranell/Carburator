package com.carbonara.game.object.spaceship;

import com.carbonara.game.object.spaceship.abstracts.AbstractSpaceShip;
import com.jme3.scene.Node;
import com.jme3.scene.Spatial;

public class CreateTestSpaceShip extends AbstractSpaceShip {
    public CreateTestSpaceShip(Spatial SpaceShipSpatial, Node spaceNode) {
        super(SpaceShipSpatial, spaceNode);
    }
}
